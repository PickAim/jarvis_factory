[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvis_factory"
version = "0.1.3"
dependencies = [
    "jarvis_calc@ git+https://github.com/PickAim/jarvis_calc.git@v.0.1.5",
    "jarvis_db@ git+https://github.com/PickAim/jarvis_db.git@v.0.1.8",
    "jdu@ git+https://github.com/PickAim/jdu.git@v.0.1.4"
]
